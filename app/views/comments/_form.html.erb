<%= simple_form_for([ article, comment ], remote: true) do |f| %>
  <%= f.input :content, label: false, placeholder: "Votre commentaire..." %>
  <div class="d-flex justify-content-end">
    <%= f.submit "Envoyer", class:"btn btn-primary bold" %>
  </div>
<% end %>
