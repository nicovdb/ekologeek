<div class="container bg-white">
  <h1 class="mt-5 mb-4 header text-primary text-center">DIAGNOSTIC COMPORTEMENTAL INDIVIDUEL</h1>


  <p class="text-center mt-4">Progression : <b>page 2 sur 5</b> </p>
  <div class="dots d-flex justify-content-center">
    <span class="blue"></span>
    <span class="blue"></span>
    <span class=""></span>
    <span class=""></span>
    <span class=""></span>
  </div>

  <%= simple_form_for(@user_behaviour_diag, method: :put, url: wizard_path) do |f| %>
    <p class="mt-4"><b>7. </b>Vous avez connaissance du contexte des déchets (enjeux, problématiques…) :</p>
    <%= f.collection_radio_buttons :context_knowledge, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>
    <% if @user_behaviour_diag.errors.include?(:context_knowledge) %>
      <p class="has-error">La question 7 est obligatoire</p>
    <% end %>

    <p class="mt-4"><b>7.1 </b>Dites nous en plus</p>
    <%= f.input :context_knowledge_comment,label: false, placeholder: "Réponse courte"%>

    <p class="mt-4"><b>8. </b>Vous vous sentez concerné·e par la thématique des déchets :</p>
    <%= f.collection_radio_buttons :concerned, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>
    <% if @user_behaviour_diag.errors.include?(:concerned) %>
      <p class="has-error">La question 8 est obligatoire</p>
    <% end %>

    <p class="mt-4"><b>9. </b>Vous êtes favorables à la réduction des déchets :</p>
    <%= f.collection_radio_buttons :favorable_reduction, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>

    <p class="mt-4"><b>10. </b>La réduction des déchets vous semble embêtante ?</p>
    <%= f.collection_radio_buttons :crappy_reduction, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>

    <p class="mt-4"><b>11. </b>La réduction des déchets de fonctionnement sur votre lieu du travail vous semble difficile :</p>
    <%= f.collection_radio_buttons :trash_reduction_hard, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>
    <% if @user_behaviour_diag.errors.include?(:trash_reduction_hard) %>
      <p class="has-error">La question 11 est obligatoire</p>
    <% end %>

    <p class="mt-4"><b>12. </b>La présence d’une personne ressource sur la question des déchets de fonctionnement dans votre structure vous semble inutile :</p>
    <%= f.collection_radio_buttons :dedicated_employee, ["Pas du tout", "Un peu", "Moyennement", "Beaucoup",  "Tout à fait", "Non concerné"], :to_s, :to_s do |input| %>
      <label class="mx-2 mb-3">
        <%= input.radio_button %>
        <%= input.text %>
      </label>
    <% end %>

    <div class="d-flex justify-content-around mb-5 mt-4">
      <%= link_to "< Précédent", previous_wizard_path, class:"btn btn-primary bold" %>
      <%= f.button :submit, "Suivant >", class:"btn btn-primary bold" %>
    </div>

  <% end %>

</div>
