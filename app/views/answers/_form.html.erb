<%= simple_form_for([ topic, answer ], remote: true) do |f| %>
  <%= f.input :content, label: false, placeholder: "Votre commentaire..." %>
  <%= f.submit "Envoyer", class: 'btn btn-primary' %>
<% end %>

